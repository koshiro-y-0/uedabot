⚠️ 【為替アラート】{{ timestamp }}
━━━━━━━━━━━━━━━━━━━━
USD/JPY が急変しました！

  前回チェック時：{{ "%.2f"|format(last) }}円
  現在値：{{ "%.2f"|format(current) }}円
  変動幅：{{ "%+.2f"|format(diff) }}円（{{ "%+.2f"|format(pct) }}%）

📊 トレンド：急激な{{ direction }}方向
「為替市場の急変に際しては、
 適切に対応してまいります。」
━━━━━━━━━━━━━━━━━━━━