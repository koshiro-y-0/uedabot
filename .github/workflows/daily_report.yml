name: 植田総裁Bot - 毎朝レポート配信

on:
  # 平日 08:30 JST (= UTC 23:30 前日) ※GitHub Actions遅延を考慮し23:20に設定
  schedule:
    - cron: "20 23 * * 0-4"
  # 手動実行（テスト・デバッグ用）
  workflow_dispatch:

jobs:
  send-daily-report:
    runs-on: ubuntu-latest

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Python 3.11 をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: 依存ライブラリをインストール
        run: pip install -r requirements.txt

      - name: 経済指標を取得してレポートを送信
        env:
          ESTAT_API_KEY: ${{ secrets.ESTAT_API_KEY }}
          LINE_CHANNEL_TOKEN: ${{ secrets.LINE_CHANNEL_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python src/main.py

      - name: 日次データCSVをコミット・プッシュ
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/daily_indicators.csv
          git diff --cached --quiet || git commit -m "data: $(date +%Y-%m-%d) の日次指標データを保存"
          git push
